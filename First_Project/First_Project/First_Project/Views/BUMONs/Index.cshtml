@model IEnumerable<First_Project.Models.BUMON>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BUMONNM)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BUMONNM)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BUMONCD">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BUMONCD">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BUMONCD">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
